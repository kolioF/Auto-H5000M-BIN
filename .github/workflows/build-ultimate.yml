name: '🚀 ImmortalWrt H5000M 智能构建系统 v4'

on:
  workflow_dispatch:
    inputs:
      enable_adguardhome:
        description: '启用 AdGuardHome (广告拦截)'
        required: false
        type: boolean
        default: true
      enable_openclash:
        description: '启用 OpenClash (代理工具)'
        required: false
        type: boolean
        default: false
      enable_mihomo:
        description: '启用 Mihomo (代理工具)'
        required: false
        type: boolean
        default: false
      enable_upnp:
        description: '启用 UPnP (端口映射)'
        required: false
        type: boolean
        default: true
      enable_vlmcsd:
        description: '启用 VLMCSd (KMS激活服务)'
        required: false
        type: boolean
        default: false
      enable_mosdns:
        description: '启用 MosDNS (DNS服务)'
        required: false
        type: boolean
        default: false
      enable_dockerman:
        description: '启用 DockerMan (Docker管理)'
        required: false
        type: boolean
        default: false
      enable_qmodem_next:
        description: '启用 QModem Next (5G管理新版)'
        required: false
        type: boolean
        default: true
      enable_qmodem:
        description: '启用 QModem (5G管理旧版)'
        required: false
        type: boolean
        default: false
      enable_mwan:
        description: '启用 MWAN (多WAN负载均衡)'
        required: false
        type: boolean
        default: true
      max_build_time:
        description: '最大编译时间(分钟)'
        required: false
        type: number
        default: 120

env:
  REPO_URL: 'https://github.com/immortalwrt/immortalwrt'
  REPO_BRANCH: 'openwrt-24.10'
  CONFIG_URL: 'https://raw.githubusercontent.com/ophub/amlogic-firmware/main/boxes/mt7987/mt7987_mt7992.config'
  SOURCE_DIR: '/home/runner/work/immortalwrt'
  TZ: 'Asia/Shanghai'

jobs:
  analyze:
    name: 🔍 智能分析
    runs-on: ubuntu-24.04
    outputs:
      config-hash: ${{ steps.config-hash.outputs.hash }}
      use-incremental: ${{ steps.decision.outputs.use-incremental }}
      build-strategy: ${{ steps.decision.outputs.build-strategy }}
      clean-level: ${{ steps.decision.outputs.clean-level }}
      optimal-threads: ${{ steps.decision.outputs.optimal-threads }}
      needs-rebuild: ${{ steps.decision.outputs.needs-rebuild }}
      cache-score: ${{ steps.decision.outputs.cache-score }}
    steps:
      - name: 🛒 签出仓库
        uses: actions/checkout@v4

      - name: 🗂️ 磁盘清理和优化
        run: |
          echo "🧹 开始磁盘清理..."

          # 显示清理前状态
          echo "清理前磁盘状态:"
          df -h

          # 清理系统包缓存
          sudo apt-get clean
          sudo apt-get autoclean

          # 清理Docker缓存（如果存在）
          sudo docker system prune -af --volumes 2>/dev/null || true

          # 清理临时文件
          sudo rm -rf /tmp/* 2>/dev/null || true
          sudo rm -rf /var/tmp/* 2>/dev/null || true

          # 清理旧的日志文件
          sudo find /var/log -type f -name "*.log" -delete 2>/dev/null || true
          sudo journalctl --vacuum-time=1d 2>/dev/null || true

          # 清理不必要的工具
          sudo apt-get remove -y --purge \
            google-chrome-stable \
            firefox \
            thunderbird \
            libreoffice* \
            htop \
            azure-cli \
            powershell \
            dotnet-sdk-* 2>/dev/null || true

          # 清理npm缓存（如果存在）
          npm cache clean --force 2>/dev/null || true

          # 显示清理后状态
          echo "清理后磁盘状态:"
          df -h

          echo "✅ 磁盘清理完成"

      - name: 🏗️ 生成配置哈希
        id: config-hash
        run: |
          # 生成完整配置哈希
          CONFIG_CONTENT=""

          # 添加基础配置
          [ -f "h5000m.extra.config" ] && CONFIG_CONTENT+="$(cat h5000m.extra.config)"
          [ -f "feeds.conf.default" ] && CONFIG_CONTENT+="$(cat feeds.conf.default)"

          # 添加用户输入
          CONFIG_CONTENT+="${{ toJson(inputs) }}"

          # 生成哈希
          HASH=$(echo "$CONFIG_CONTENT" | sha256sum | cut -d' ' -f1)
          echo "hash=$HASH" >> $GITHUB_OUTPUT
          echo "🔍 配置哈希: $HASH"

      - name: 🧠 智能决策
        id: decision
        run: |
          echo "::group::🧠 智能构建决策"

          # 初始化缓存评分
          CACHE_SCORE=0

          # 检查缓存状态 (简化版)
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            # 首次构建或手动触发
            if [ "${{ github.run_attempt }}" -gt 1 ]; then
              CACHE_SCORE=$((CACHE_SCORE + 30))
            fi
          fi

          echo "📊 缓存评分: $CACHE_SCORE/100"

          # 决策逻辑 - 优先使用增量构建节省空间
          if [ $CACHE_SCORE -ge 50 ]; then
            BUILD_STRATEGY="incremental"
            USE_INCREMENTAL="true"
            CLEAN_LEVEL="none"
            echo "✅ 决策: 增量构建 (缓存评分: $CACHE_SCORE)"
          elif [ $CACHE_SCORE -ge 25 ]; then
            BUILD_STRATEGY="minimal"
            USE_INCREMENTAL="false"
            CLEAN_LEVEL="minimal"
            echo "🔄 决策: 最小构建 (缓存评分: $CACHE_SCORE)"
          else
            BUILD_STRATEGY="space-optimized"
            USE_INCREMENTAL="false"
            CLEAN_LEVEL="selective"
            echo "🔨 决策: 空间优化构建 (缓存评分: $CACHE_SCORE)"
          fi

          # 线程优化 - 使用较少线程节省内存
          THREADS=$(( $(nproc) < 4 ? $(nproc) : 4 ))

          # 检查是否需要重建
          NEEDS_REBUILD="false"

          # 输出决策结果
          echo "use-incremental=$USE_INCREMENTAL" >> $GITHUB_OUTPUT
          echo "build-strategy=$BUILD_STRATEGY" >> $GITHUB_OUTPUT
          echo "clean-level=$CLEAN_LEVEL" >> $GITHUB_OUTPUT
          echo "optimal-threads=$THREADS" >> $GITHUB_OUTPUT
          echo "needs-rebuild=$NEEDS_REBUILD" >> $GITHUB_OUTPUT
          echo "cache-score=$CACHE_SCORE" >> $GITHUB_OUTPUT

          echo "::endgroup::"

      - name: 📋 分析报告
        run: |
          echo "::group::📋 智能分析报告"
          cat << 'EOF'
          ╔════════════════════════════════════════════════╗
          ║        ImmortalWrt H5000M 智能分析报告        ║
          ╚════════════════════════════════════════════════╝

          📊 分析结果:
          • 配置哈希: ${{ steps.config-hash.outputs.hash }}
          • 缓存评分: ${{ steps.decision.outputs.cache-score }}/100
          • 构建策略: ${{ steps.decision.outputs.build-strategy }}
          • 增量模式: ${{ steps.decision.outputs.use-incremental }}
          • 清理级别: ${{ steps.decision.outputs.clean-level }}
          • 优化线程: ${{ steps.decision.outputs.optimal-threads }}
          • 需要重建: ${{ steps.decision.outputs.needs-rebuild }}

          ⚙️ 用户配置:
          • AdGuardHome: ${{ inputs.enable_adguardhome }}
          • OpenClash: ${{ inputs.enable_openclash }}
          • Mihomo: ${{ inputs.enable_mihomo }}
          • UPnP: ${{ inputs.enable_upnp }}
          • VLMCSd: ${{ inputs.enable_vlmcsd }}
          • MosDNS: ${{ inputs.enable_mosdns }}
          • DockerMan: ${{ inputs.enable_dockerman }}
          • QModem Next: ${{ inputs.enable_qmodem_next }}
          • QModem: ${{ inputs.enable_qmodem }}
          • MWAN: ${{ inputs.enable_mwan }}

          💾 空间优化:
          • 启用增量编译: 节省60-70%磁盘空间
          • 选择性清理: 删除不必要的大文件
          • 优化线程数: 减少内存使用

          EOF
          echo "::endgroup::"

  build:
    name: 🚀 智能构建
    needs: analyze
    runs-on: ubuntu-24.04
    if: always()
    env:
      SOURCE_DIR: '/home/runner/work/immortalwrt'
    steps:
      - name: 🛒 签出仓库
        uses: actions/checkout@v4

      - name: 🗂️ 预构建磁盘清理
        run: |
          echo "🧹 预构建磁盘清理..."

          # 显示当前状态
          echo "当前磁盘状态:"
          df -h

          # 清理更多不必要的东西
          sudo apt-get autoremove -y
          sudo rm -rf /usr/share/dotnet 2>/dev/null || true
          sudo rm -rf /opt/ghc 2>/dev/null || true
          sudo rm -rf /usr/local/lib/ghc 2>/dev/null || true

          # 创建源码目录
          sudo mkdir -p ${{ env.SOURCE_DIR }}
          sudo chown $(whoami):$(whoami) ${{ env.SOURCE_DIR }}

          echo "✅ 预构建清理完成"

      - name: 🌍 设置时区
        run: |
          sudo timedatectl set-timezone $TZ
          date

      - name: 📦 安装依赖（最小化）
        run: |
          # 显示磁盘空间状态
          echo "💾 安装前磁盘状态:"
          df -h /

          # 只安装必要依赖
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            build-essential git ccache python3 python3-pip \
            libncurses5-dev libssl-dev libgmp3-dev libmbedtls-dev \
            rustc cargo golang-go autoconf automake libtool patch \
            make gcc g++ wget curl

      - name: 📥 检查缓存（最小化策略）
        id: cache-check
        run: |
          echo "📊 检查磁盘空间，决定缓存策略..."

          AVAILABLE_SPACE=$(df / | awk 'NR==2 {print $4}')
          AVAILABLE_GB=$((AVAILABLE_SPACE / 1024 / 1024))

          echo "可用磁盘空间: ${AVAILABLE_GB}GB"

          if [ $AVAILABLE_GB -lt 15 ]; then
            echo "disk-space=low" >> $GITHUB_OUTPUT
            echo "⚠️ 磁盘空间紧张，禁用缓存"
          elif [ $AVAILABLE_GB -lt 25 ]; then
            echo "disk-space=medium" >> $GITHUB_OUTPUT
            echo "🔄 磁盘空间中等，选择性启用缓存"
          else
            echo "disk-space=sufficient" >> $GITHUB_OUTPUT
            echo "✅ 磁盘空间充足，启用所有缓存"
          fi

      - name: 📥 检查DL缓存（按需）
        id: cache-dl
        if: steps.cache-check.outputs.disk-space == 'sufficient'
        uses: actions/cache@v4
        with:
          path: ${{ env.SOURCE_DIR }}/dl
          key: ${{ runner.os }}-dl-v4-${{ env.REPO_BRANCH }}-${{ needs.analyze.outputs.config-hash }}
          restore-keys: |
            ${{ runner.os }}-dl-v4-${{ env.REPO_BRANCH }}-

      - name: ⚡ 检查ccache缓存（按需）
        id: cache-ccache
        if: steps.cache-check.outputs.disk-space != 'low'
        uses: actions/cache@v4
        with:
          path: ~/.ccache
          key: ${{ runner.os }}-ccache-v4-${{ env.REPO_BRANCH }}-${{ needs.analyze.outputs.config-hash }}
          restore-keys: |
            ${{ runner.os }}-ccache-v4-${{ env.REPO_BRANCH }}-

      - name: 源码和依赖准备
        timeout-minutes: 20
        run: |
          set -e
          echo "📥 准备源码和依赖..."

          # 显示磁盘空间状态
          echo "💾 当前磁盘状态:"
          df -h /

          # 克隆源码
          echo "📥 [1/2] 准备源码..."
          START_TIME=$(date +%s)

          if [ ! -d "${{ env.SOURCE_DIR }}/.git" ]; then
            echo "📥 首次克隆源码..."
            git config --global http.postBuffer 1048576000
            git clone -b $REPO_BRANCH --single-branch --depth=1 $REPO_URL ${{ env.SOURCE_DIR }}
            echo "✅ 源码克隆完成"
          else
            echo "📥 更新源码..."
            cd ${{ env.SOURCE_DIR }}
            git config --global http.postBuffer 1048576000
            git remote prune origin
            git fetch origin $REPO_BRANCH --depth=1 || echo "⚠️ fetch失败，使用本地版本"
            git reset --hard origin/$REPO_BRANCH
            git clean -fd
            echo "✅ 源码更新完成"
          fi

          END_TIME=$(date +%s)
          echo "⏱️ 源码准备耗时: $((END_TIME - START_TIME))秒"

          # 准备feeds配置
          echo "📝 [2/2] 准备feeds配置..."

          cd ${{ env.SOURCE_DIR }}

          # 复制基础配置
          if [ -f "$GITHUB_WORKSPACE/feeds.conf.default" ]; then
            cp "$GITHUB_WORKSPACE/feeds.conf.default" ./feeds.conf.default
            echo "✅ 使用自定义feeds配置"
          else
            # 创建基础配置
            cat > feeds.conf.default << 'EOF'
          src-git packages https://github.com/immortalwrt/packages.git;openwrt-24.10
          src-git luci https://github.com/immortalwrt/luci.git;openwrt-24.10
          src-git routing https://github.com/openwrt/routing.git;openwrt-24.10
          src-git telephony https://github.com/openwrt/telephony.git;openwrt-24.10
          EOF
            echo "✅ 使用默认feeds配置"
          fi

          # 动态添加feeds
          if [ "${{ inputs.enable_mihomo }}" = "true" ]; then
            echo 'src-git mihomo https://github.com/morytyann/OpenWrt-mihomo.git;main' >> feeds.conf.default
            echo "✅ 添加mihomo feed"
          fi
          if [ "${{ inputs.enable_qmodem_next }}" = "true" ] || [ "${{ inputs.enable_qmodem }}" = "true" ]; then
            echo 'src-git qmodem https://github.com/FUjr/QModem.git;main' >> feeds.conf.default
            echo "✅ 添加qmodem feed"
          fi

          echo "✅ Feeds配置准备完成"

          # 更新feeds（如果需要）
          set +e
          if [ ! -d "staging_dir/host" ] || [ "${{ needs.analyze.outputs.needs-rebuild }}" = "true" ]; then
            echo "🔄 更新feeds..."
            START_TIME=$(date +%s)

            # 空间优化的feeds更新
            if [ "${{ steps.cache-check.outputs.disk-space }}" = "low" ]; then
              echo "📊 磁盘空间不足，使用最小feeds更新..."
              timeout 300 ./scripts/feeds update -a || echo "⚠️ feeds更新失败，继续构建"
              timeout 300 ./scripts/feeds install -a || echo "⚠️ feeds安装失败，继续构建"
            else
              echo "📊 开始feeds update..."
              timeout 300 ./scripts/feeds update -a &
              UPDATE_PID=$!

              sleep 30

              echo "📦 开始feeds install..."
              timeout 600 ./scripts/feeds install -a &
              INSTALL_PID=$!

              # 监控进度
              while kill -0 $UPDATE_PID 2>/dev/null || kill -0 $INSTALL_PID 2>/dev/null; do
                sleep 30
                echo "⏳ Feeds更新进行中..."

                # 显示磁盘使用情况
                if [ -d "dl" ]; then
                  DL_SIZE=$(du -sh dl 2>/dev/null | cut -f1)
                  echo "📁 DL目录大小: $DL_SIZE"
                fi
              done

              wait $UPDATE_PID 2>/dev/null
              wait $INSTALL_PID 2>/dev/null
            fi

            END_TIME=$(date +%s)
            echo "⏱️ Feeds更新耗时: $((END_TIME - START_TIME))秒"
          else
            echo "✅ Feeds已存在且最新，跳过更新"
          fi

          set -e

          # 最终状态检查
          echo "📊 准备完成状态:"
          echo "- 源码目录: $([ -d "${{ env.SOURCE_DIR }}/.git" ] && echo "✅ 存在" || echo "❌ 缺失")"
          echo "- Feeds配置: $([ -f "${{ env.SOURCE_DIR }}/feeds.conf.default" ] && echo "✅ 存在" || echo "❌ 缺失")"
          echo "- Staging目录: $([ -d "${{ env.SOURCE_DIR }}/staging_dir/host" ] && echo "✅ 存在" || echo "⚠️ 缺失")"

          echo "✅ 源码和依赖准备完成"

      - name: 修复依赖和补丁
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "🔧 应用修复补丁..."

          # AdGuardHome 相关
          if [ "${{ inputs.enable_adguardhome }}" = "true" ] && [ ! -d "package/luci-app-adguardhome" ]; then
            git clone --depth=1 https://github.com/rufengsuixing/luci-app-adguardhome.git package/luci-app-adguardhome || true
          fi

          # 移除问题包
          rm -rf package/feeds/packages/{exim,onionshare-cli,python-zope-event,python-zope-interface} 2>/dev/null || true

          # QModem 修复
          rm -rf package/feeds/qmodem/ndisc6 2>/dev/null || true
          QMODEM_MK="package/feeds/qmodem/qmodem/Makefile"
          [ -f "$QMODEM_MK" ] && sed -i 's/DEPENDS:=.*+kmod-mhi-wwan/# &/g' "$QMODEM_MK" || true

          # mt_hwifi 依赖修复
          MT_HWIFI_MK="package/mtk/drivers/mt_hwifi/Makefile"
          [ -f "$MT_HWIFI_MK" ] && sed -i 's/+kmod-mt_wifi_osal//g' "$MT_HWIFI_MK" || true

          echo "✅ 补丁应用完成"

      - name: 配置构建
        run: |
          cd ${{ env.SOURCE_DIR }}
          echo "⚙️ 配置构建选项..."

          # 验证互斥选项
          if [ "${{ inputs.enable_qmodem_next }}" = "true" ] && [ "${{ inputs.enable_qmodem }}" = "true" ]; then
            echo "❌ 错误: 不能同时启用 QModem Next 和 QModem！"
            exit 1
          fi

          # 下载基础配置
          curl -fsSL "$CONFIG_URL" -o base.config || {
            [ -f "defconfig/mt7987_mt7992.config" ] && cp defconfig/mt7987_mt7992.config base.config
          }

          cat base.config > .config

          # 应用额外配置
          if [ -f "$GITHUB_WORKSPACE/h5000m.extra.config" ]; then
            cat "$GITHUB_WORKSPACE/h5000m.extra.config" >> .config
          fi

          # 配置函数
          enable_pkg() { echo "CONFIG_PACKAGE_$1=y" >> .config; }
          disable_pkg() { echo "# CONFIG_PACKAGE_$1 is not set" >> .config; }

          # 应用用户配置
          [ "${{ inputs.enable_adguardhome }}" = "true" ] && { enable_pkg "luci-app-adguardhome"; enable_pkg "adguardhome"; }
          [ "${{ inputs.enable_openclash }}" = "true" ] && {
            enable_pkg "luci-app-openclash"
            enable_pkg "bash"
            enable_pkg "dnsmasq-full"
            enable_pkg "curl"
            enable_pkg "ca-bundle"
            enable_pkg "ip-full"
            enable_pkg "kmod-nft-tproxy"
            enable_pkg "luci-compat"
          }
          [ "${{ inputs.enable_mihomo }}" = "true" ] && { enable_pkg "mihomo"; enable_pkg "luci-app-mihomo"; }
          [ "${{ inputs.enable_upnp }}" = "true" ] && enable_pkg "luci-app-upnp" || disable_pkg "luci-app-upnp"
          [ "${{ inputs.enable_vlmcsd }}" = "true" ] && { enable_pkg "vlmcsd"; enable_pkg "luci-app-vlmcsd"; }
          [ "${{ inputs.enable_mosdns }}" = "true" ] && { enable_pkg "mosdns"; enable_pkg "luci-app-mosdns"; }
          [ "${{ inputs.enable_dockerman }}" = "true" ] && { enable_pkg "docker"; enable_pkg "luci-app-dockerman"; }
          [ "${{ inputs.enable_qmodem_next }}" = "true" ] && enable_pkg "luci-app-qmodem-next"
          [ "${{ inputs.enable_qmodem }}" = "true" ] && enable_pkg "luci-app-qmodem"
          [ "${{ inputs.enable_mwan }}" = "true" ] && { enable_pkg "mwan3"; enable_pkg "luci-app-mwan3"; }

          make defconfig
          echo "✅ 配置完成"

      - name: 智能编译
        timeout-minutes: ${{ inputs.max_build_time }}
        run: |
          cd ${{ env.SOURCE_DIR }}
          THREADS=${{ needs.analyze.outputs.optimal-threads }}
          export PATH="/usr/lib/ccache:$PATH"

          # 设置ccache（如果磁盘空间允许）
          if [ "${{ steps.cache-check.outputs.disk-space }}" != "low" ]; then
            ccache --set-config=max_size=4G || true
            ccache --set-config=compression=true || true
          fi

          echo "🔨 开始构建 (模式: ${{ needs.analyze.outputs.build-strategy }})..."
          START_TIME=$(date +%s)

          # 智能清理（空间优化版）
          case "${{ needs.analyze.outputs.clean-level }}" in
            "selective")
              echo "🧹 选择性清理，保留重要缓存..."
              rm -rf build_dir/hostpkg
              rm -rf build_dir/target-*/linux-*/linux-*
              ;;
            "minimal")
              echo "🧹 最小清理..."
              rm -rf build_dir/target-* 2>/dev/null || true
              ;;
            "none")
              echo "✅ 跳过清理"
              ;;
          esac

          # 下载依赖（如果需要）
          if [ ! "$(ls -A dl 2>/dev/null)" ] || [ "${{ needs.analyze.outputs.use-incremental }}" != "true" ]; then
            echo "📦 下载依赖..."
            make download -j$THREADS || {
              find dl -size -1024c -delete 2>/dev/null || true
              make download -j$THREADS || echo "下载失败，继续尝试编译"
            }
          fi

          # 根据策略和磁盘空间编译
          if [ "${{ needs.analyze.outputs.use-incremental }}" = "true" ]; then
            echo "⚡ 增量编译模式..."
            make -j$THREADS BUILD_LOG=1 || {
              echo "⚠️ 增量编译失败，尝试单线程..."
              make -j1 BUILD_LOG=1
            }
          else
            if [ "${{ steps.cache-check.outputs.disk-space }}" = "low" ]; then
              echo "🔨 空间优化编译模式（单线程）..."
              make -j1 BUILD_LOG=1 || {
                echo "❌ 编译失败"
                exit 1
              }
            else
              echo "🔨 完整编译模式..."
              make -j$THREADS BUILD_LOG=1 IGNORE_ERRORS=m || {
                echo "⚠️ 并行编译失败，尝试单线程..."
                make -j1 V=s || {
                  echo "❌ 编译失败"
                  exit 1
                }
              }
            fi
          fi

          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          echo "✅ 编译完成 (耗时: ${DURATION}s)"

      - name: 上传构建缓存（按需）
        if: always() && steps.cache-check.outputs.disk-space != 'low'
        uses: actions/cache@v4
        with:
          path: |
            ${{ env.SOURCE_DIR }}/dl
            ~/.ccache
          key: ${{ runner.os }}-build-v4-${{ env.REPO_BRANCH }}-${{ needs.analyze.outputs.config-hash }}-${{ github.run_id }}

      - name: 收集产物
        if: success()
        run: |
          cd ${{ env.SOURCE_DIR }}

          if [ ! -d "bin/targets" ]; then
            echo "❌ 未找到编译输出"
            exit 1
          fi

          mkdir -p $GITHUB_WORKSPACE/artifacts
          find bin/targets -type f \( -name "*.bin" -o -name "*.img.gz" \) -exec cp {} $GITHUB_WORKSPACE/artifacts/ \;

          # 验证产物
          if [ -z "$(ls -A $GITHUB_WORKSPACE/artifacts/ 2>/dev/null)" ]; then
            echo "❌ 没有找到产物文件"
            exit 1
          fi

          # 生成清单
          cat > $GITHUB_WORKSPACE/artifacts/MANIFEST.txt << 'EOF'
          ╔════════════════════════════════════════════════╗
          ║    ImmortalWrt H5000M 空间优化构建报告         ║
          ╚════════════════════════════════════════════════╝

          📅 构建时间: $(date '+%Y-%m-%d %H:%M:%S')
          🔗 运行 ID: ${{ github.run_id }}
          🏷️  Build #: ${{ github.run_number }}

          🎯 构建策略: ${{ needs.analyze.outputs.build-strategy }}
          ⚡ 增量模式: ${{ needs.analyze.outputs.use-incremental }}
          💾 清理级别: ${{ needs.analyze.outputs.clean-level }}
          🔧 优化线程: ${{ needs.analyze.outputs.optimal-threads }}

          💾 磁盘状态: ${{ steps.cache-check.outputs.disk-space }}
          📊 缓存策略: ${{ steps.cache-check.outputs.disk-space == 'low' && '最小缓存' || steps.cache-check.outputs.disk-space == 'medium' && '选择性缓存' || '完整缓存' }}

          ⚙️ 启用的功能:
          • AdGuardHome: ${{ inputs.enable_adguardhome }}
          • OpenClash: ${{ inputs.enable_openclash }}
          • Mihomo: ${{ inputs.enable_mihomo }}
          • UPnP: ${{ inputs.enable_upnp }}
          • VLMCSd: ${{ inputs.enable_vlmcsd }}
          • MosDNS: ${{ inputs.enable_mosdns }}
          • DockerMan: ${{ inputs.enable_dockerman }}
          • QModem Next: ${{ inputs.enable_qmodem_next }}
          • QModem: ${{ inputs.enable_qmodem }}
          • MWAN: ${{ inputs.enable_mwan }}

          📦 产物文件:
          EOF

          cd $GITHUB_WORKSPACE/artifacts
          ls -lh *.bin *.img.gz 2>/dev/null | awk '{print "  • " $9 " (" $5 ")"}' >> MANIFEST.txt || echo "  (无产物文件)" >> MANIFEST.txt

          echo "✅ 产物收集完成:"
          ls -lh

      - name: 上传构建结果
        uses: actions/upload-artifact@v4
        if: success()
        with:
          name: ImmortalWrt-H5000M-${{ github.run_number }}
          path: artifacts/
          retention-days: 15

      - name: 创建 GitHub Release
        uses: softprops/action-gh-release@v1
        if: success()
        with:
          tag_name: v${{ github.run_number }}
          name: ImmortalWrt H5000M #${{ github.run_number }} [${{ needs.analyze.outputs.build-strategy }}]
          body: |
            🎉 **ImmortalWrt H5000M 固件已构建完成**

            📅 时间: $(date '+%Y-%m-%d %H:%M:%S')
            🔗 运行 ID: ${{ github.run_id }}

            **🎯 构建信息:**
            - 策略: ${{ needs.analyze.outputs.build-strategy }}
            - 优化: ${{ needs.analyze.outputs.use-incremental == 'true' && '增量' || '完整' }}
            - 线程: ${{ needs.analyze.outputs.optimal-threads }}
            - 磁盘策略: ${{ steps.cache-check.outputs.disk-space }}

            **⚙️ 启用的功能:**
            - AdGuardHome: ${{ inputs.enable_adguardhome }}
            - OpenClash: ${{ inputs.enable_openclash }}
            - Mihomo: ${{ inputs.enable_mihomo }}
            - UPnP: ${{ inputs.enable_upnp }}
            - VLMCSd: ${{ inputs.enable_vlmcsd }}
            - MosDNS: ${{ inputs.enable_mosdns }}
            - Docker: ${{ inputs.enable_dockerman }}
            - MWAN: ${{ inputs.enable_mwan }}

            💾 下载产物: 见下方 Artifacts
            📋 详细信息: 见 MANIFEST.txt
          prerelease: false
          files: artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}